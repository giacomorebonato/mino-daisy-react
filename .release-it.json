{
  "git": {
    "commitMessage": "Release ${version}",
    "tagName": "${version}",
    "push": true
  },
  "npm": {
    "publish": true,
    "skipChecks": true
  },
  "github": {
    "release": true,
    "releaseName": "Release ${version}"
  },
  "hooks": {
    "before:init": ["bun run build"]
  }
}
